#!/usr/bin/env ruby

require 'docker_stats_api'

endpoint = ARGV[0]
client = DockerStatsApi::Client.new(endpoint)

response_code = client.post_stats

if response_code == 200
  puts "Successfully posted Docker stats to #{endpoint}"
else
  puts "Error posting Docker stats: HTTP #{response_code}"
  exit 1
end
